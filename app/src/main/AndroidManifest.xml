<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TP2Capitales"
        tools:targetApi="31">
        <activity
            android:name=".EliminarCapital"
            android:exported="false"
            android:label="@string/title_activity_eliminar_capital"
            android:theme="@style/Theme.TP2Capitales" />
        <activity
            android:name=".VerCapital"
            android:exported="false"
            android:label="@string/title_activity_ver_capital"
            android:theme="@style/Theme.TP2Capitales" />
        <activity
            android:name=".CargarCapital"
            android:exported="false"
            android:label="@string/title_activity_cargar_capital"
            android:theme="@style/Theme.TP2Capitales" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TP2Capitales">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>